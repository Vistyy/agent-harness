# Delivery Map (Waves + Backlog)

The wave brief `**Status:**` is the execution gate.

## Wave Plan

- `harness-thin-design-judge-contract-1` - `discovery-required`; slim
  reusable UI/design ownership so project-local design sources own method/taste
  while the harness keeps only a thin blocking `design_judge` visual gate and
  separate runtime-evidence behavior proof.

## Current Evidence

None.

## Deferred Backlog

None.
