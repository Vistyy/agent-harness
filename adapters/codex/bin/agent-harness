#!/usr/bin/env bash
set -euo pipefail

SOURCE="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd -- "$(dirname -- "$SOURCE")" && pwd)"
ROOT="$(cd -- "$SCRIPT_DIR/../../.." && pwd)"

PYTHONPATH="$ROOT${PYTHONPATH:+:$PYTHONPATH}" python3 -m agent_harness.cli "$@"
