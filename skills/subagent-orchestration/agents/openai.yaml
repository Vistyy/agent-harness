interface:
  display_name: "Subagent Orchestration"
  short_description: "Delegate bounded work cleanly"
  default_prompt: "Use $subagent-orchestration to invoke named preauthorized harness subagents without user authorization: explorer, check_runner, planning_critic, implementer, quality_guard, final_reviewer, runtime_evidence, and design_judge."
