interface:
  display_name: "GitHub CLI"
  short_description: "Use gh safely for GitHub reads and writes"
  default_prompt: "Use $github-cli to inspect this GitHub PR, issue, review thread, check, or run safely with gh."
