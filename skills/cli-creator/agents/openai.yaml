interface:
  display_name: "CLI Creator"
  short_description: "Build composable CLIs for Codex"
  default_prompt: "Use $cli-creator to create a composable CLI from this source material, install it on PATH, test it from outside the source folder, and create a companion skill."
