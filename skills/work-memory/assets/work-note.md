# Queued Work Item: <item-id>

This note is memory, not authority. Picking it starts discovery through
`solution-shaping`; it does not authorize direct execution. It is queued
starting context, not active progress state.

## Queue

- problem: `<what should not be lost>`
- queue state: `<ready | waiting | deferred>`
- owner or suspected owner: `<owner>`
- next discovery move: `<smallest useful probe>`

Optional when useful:

- rank: `<only for multiple ready items>`
- dependency/blocker/wake-up condition: `<only for waiting items>`
- reason not active now: `<only when not obvious>`

## Starting Points

- `<owner/path/doc/reference>`

## Recheck During Discovery

- `<decision, risk, stale assumption, cleanup, or proof concern>`

## Possible Sequence

- `<rough next work item>`
