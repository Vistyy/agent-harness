interface:
  display_name: "Work State"
  short_description: "Maintain lightweight work state"
  default_prompt: "Use $work-memory for delivery maps, queued items, active control sheets, resumable context, incoming work, and document lifecycle state."
