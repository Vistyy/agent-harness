interface:
  display_name: "Work Memory"
  short_description: "Maintain lightweight work memory"
  default_prompt: "Use $work-memory for delivery maps, backlog details, active notes, resumable context, incoming work, and document lifecycle memory."
