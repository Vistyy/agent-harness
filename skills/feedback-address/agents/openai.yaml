interface:
  display_name: "Feedback Address"
  short_description: "Handle feedback and workflow friction"
  default_prompt: "Use $feedback-address to decide whether feedback or workflow friction needs a surface fix, owner fix, tracked separate debt, accepted temporary debt, ledger entry, or no change."
