interface:
  display_name: "Feedback Address"
  short_description: "Assess feedback before changing work"
  default_prompt: "Use $feedback-address to decide whether this feedback needs a surface fix, owner fix, backlog item, or no change."
