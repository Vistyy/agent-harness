interface:
  display_name: "Code Review"
  short_description: "Reference-gated review doctrine"
  default_prompt: "Use $code-review when the user asks for code review or when $solution-shaping needs bug, structural-quality, objective, proof, and repo-health review. Select any needed $code-review references before applying the skill body."
